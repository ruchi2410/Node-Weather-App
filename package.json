{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "creating a weather nodejs app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruchi Yadav",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "requests": "^0.3.0"
  }
}
